{
  "name": "demon-panel",
  "version": "1.0.0",
  "description": "Demon Panel - A custom game panel",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install-all": "npm install && cd frontend && npm install",
    "backend": "nodemon server.js",
    "frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["game-panel", "pterodactyl", "demon-panel"],
  "author": "M.R Glitch",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.0.3",
    "mongoose": "^7.0.3",
    "cors": "^2.8.5",
    "axios": "^1.3.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
